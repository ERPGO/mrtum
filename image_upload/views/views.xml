<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="image_upload.list">
      <field name="name">image_upload list</field>
      <field name="model">image_upload.image_upload</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="image_upload.action_window">
      <field name="name">image_upload window</field>
      <field name="res_model">image_upload.image_upload</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="image_upload.action_server">
      <field name="name">image_upload server</field>
      <field name="model_id" ref="model_image_upload_image_upload"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="image_upload" id="image_upload.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="image_upload.menu_1" parent="image_upload.menu_root"/>
    <menuitem name="Menu 2" id="image_upload.menu_2" parent="image_upload.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="image_upload.menu_1_list" parent="image_upload.menu_1"
              action="image_upload.action_window"/>
    <menuitem name="Server to list" id="image_upload" parent="image_upload.menu_2"
              action="image_upload.action_server"/>
    -->
  </data>
</odoo>